# Git LFS configuration for large media files
# This prevents GitHub warnings for files over 50MB
# Files are automatically tracked by LFS when they exceed size limits
# Track large video files with Git LFS
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
*.avi filter=lfs diff=lfs merge=lfs -text
# Track large image files (only if needed in future)
# Uncomment if you need to track large images:
# *.jpg filter=lfs diff=lfs merge=lfs -text
# *.jpeg filter=lfs diff=lfs merge=lfs -text
# *.png filter=lfs diff=lfs merge=lfs -text
# *.webp filter=lfs diff=lfs merge=lfs -text
